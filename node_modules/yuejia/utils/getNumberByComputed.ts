/** 获取样式的数值，有小数会向上加1 */
export default function getNumberByComputed(computed: string | null): number {
  const cssValue = computed || '0';
  const float = parseFloat(cssValue);

  return isNaN(float) ? 0 : Math.ceil(float);
}