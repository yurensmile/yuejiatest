/** 链接是否是图片文件 */
export default function isImgFile(src: string): boolean {
  const lower = src.toLocaleLowerCase();
  return lower.lastIndexOf('.png') > 0 || lower.lastIndexOf('.jpg') > 0 || lower.lastIndexOf('.jpeg') > 0 || lower.lastIndexOf('.gif') > 0;
}