/** 把手机号码格式化成前三后四 */
export default function telFormat(tel: string): string {
  const front = tel.substr(0, 3);
  const middle = tel.substr(3, 4);
  const back = tel.substr(7);

  return front + ' ' + middle + ' ' + back;
}