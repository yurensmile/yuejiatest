/** 数字转成大写 */
export default function intToCapital(value: number | string): string {
  return (value !== undefined && value !== null) ? String.fromCharCode(value.toString().charCodeAt(0) + 16) : '';
}