import { Data } from '../component/Picker/Item';

/** 从PickerData中获取索引值，只能一列一列的获取 */
export default function findIndexByPickerData(data: Data[], value: string | number): number {
  for (let i = 0; i < data.length; i++) {
    if (value === data[i].value) {
      return i;
    }
  }

  return 0;
}