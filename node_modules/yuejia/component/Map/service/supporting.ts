import Choice from '../../Choice/service';

class Supporting {
  public choice: Choice = new Choice({
    fieldName: '周边配套',
    fieldKey: 'supporting',
  });
  /** 周边配套半径 */
  public radius: Map<string, number> = new Map();
}

export default Supporting;