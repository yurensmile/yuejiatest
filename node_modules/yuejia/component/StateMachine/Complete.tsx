import * as React from 'react';
import AutoHeight from './AutoHeight';

interface Props {
  children: any;
  autoHeight?: boolean;
}

const Complete = (props: Props) => {
  return props.autoHeight ? (
    <AutoHeight>{props.children || null}</AutoHeight>
  ) : props.children || null;
};

export default Complete;