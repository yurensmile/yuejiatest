"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs = require("fs");
const path = require("path");
function default_1(writeParams, readFilePath) {
    const content = writeParams.writeContent || fs.readFileSync(path.resolve(__dirname, readFilePath), 'utf-8');
    fs.writeFileSync(writeParams.filepath + writeParams.filename, content, { encoding: 'utf8' });
}
exports.default = default_1;
