"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const yargs = require("yargs");
const templete_1 = require("./templete");
const file_1 = require("./file");
require('colors');
function default_1() {
    const argv = yargs.argv;
    const service = argv.service;
    const name = argv.name;
    const type = argv.type;
    let writeContent = '';
    if (service && name) {
        switch (type) {
            case 'getList':
                writeContent = templete_1.getListAction(name);
                break;
            case 'getDetail':
                writeContent = templete_1.getDetailAction(name);
                break;
            default:
                writeContent = templete_1.action(name);
                break;
        }
        file_1.default({
            filepath: `./src/app/model/${service}/`,
            filename: `${name}.ts`,
            writeContent
        });
        console.log(`${name}动作创建成功`.green);
    }
    else {
        console.log(`service和name参数必填`.red);
    }
}
exports.default = default_1;
