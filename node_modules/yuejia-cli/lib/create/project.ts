import { argv } from 'yargs';
import unzipTemplete from '../unzip/templete';
// import unzipNodeModules from '../unzip/node_modules';

export default function(): void {
  const path = argv.path;
  unzipTemplete(path);
  // unzipNodeModules(path);
}