declare namespace Cli {

  interface Package {
    router: {
      autoCreate: boolean;
      codeSplitting: boolean;
      mode: 'HashRouter' | 'BrowserRouter';
      module: ModuleConfig[]
    };
  }

  interface ModuleConfig {
    name: string;
    indexRouter: string;
  }

}

export default Cli;