import { argv } from 'yargs';
import unzip from './unzip';

export default function(rootPath?: string): void {
  const path = argv.path;
  console.log(rootPath || path);
  unzip('node_modules', rootPath || path);
}