"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs = require("fs");
const path = require("path");
const getPageData_1 = require("./getPageData");
const createXlsx_1 = require("./createXlsx");
require("./getSheetConfig");
function outputBehavior() {
    const baseDir = './src/app/view/pages';
    const pageDataMap = {};
    const data = [];
    fs.readdirSync(baseDir).forEach((filename, i) => {
        const pagePath = path.join(baseDir, filename, './index.tsx');
        const pageData = getPageData_1.default(pagePath);
        pageDataMap[filename] = pageData;
    });
    const pageDataMapKeys = Object.keys(pageDataMap);
    pageDataMapKeys.forEach((key) => {
        const pageData = pageDataMap[key];
        pageData.forEach((item) => {
            data.push(item);
        });
    });
    createXlsx_1.default(data);
}
exports.default = outputBehavior;
