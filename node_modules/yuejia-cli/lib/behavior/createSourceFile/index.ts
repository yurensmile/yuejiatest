import * as fs from 'fs';
import * as ts from 'typescript';

export default function createSourceFile(pagePath: string): ts.SourceFile {
  // 读取文件内容
  const content = fs.readFileSync(pagePath, { encoding: 'utf8' });
  return ts.createSourceFile('', content, 6, false, 4);
}